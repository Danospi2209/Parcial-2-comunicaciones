apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: database:5432
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    database: ${POSTGRES_DB}
    jsonData:
      sslmode: "disable"
    isDefault: true